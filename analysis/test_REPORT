- MARCH=NATIVE [Ricorda: controparte per ICX -> xHost]

Con -march=native, dici al compilatore:  "Guarda esattamente che CPU ho sotto il cofano e usa ogni singola arma a sua disposizione".
Ecco cosa succede nello specifico:
    1. Attivazione delle istruzioni SIMD (AVX/AVX2/AVX-512)
    I processori moderni hanno registri molto larghi (256 o 512 bit). Invece di moltiplicare un double alla volta, possono caricarne 4 o 8 contemporaneamente.
    Senza -march=native: Il compilatore usa istruzioni generiche (SSE) che sono meno esigenti sull'allineamento ma più lente.
    Con -march=native: Il compilatore attiva le istruzioni AVX. Queste istruzioni sono come "autostrade a 8 corsie": se le macchine (i dati) entrano tutte allineate (vmovapd), viaggiano a velocità folle. 
                        Se sono disallineate, il processore deve fare un lavoro extra di "riordino" che annulla il vantaggio.

    2. Perché aligned_alloc ora vince su malloc?
    Ora che il compilatore sta usando queste istruzioni "pesanti", l'allineamento diventa critico.
    Con malloc: Anche se l'indirizzo è spesso allineato, il compilatore non può esserne sicuro a priori. Per sicurezza, deve generare istruzioni di caricamento "prudente" (vmovupd), che gestiscono i dati come se fossero disallineati.
    Con aligned_alloc: Hai dato al compilatore la garanzia formale. Lui sa che può "spingere sull'acceleratore" usando le istruzioni di caricamento "veloce" (vmovapd).

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

- LOOP TILING
Hint: To perform loop tiling, the loops have to be perfectly nested and a certain type of loops with loop-carried dependencies cannot be tiled.

- MIN - min(...)
Se la tua matrice è $100 \times 100$ e il tuo blocco è $32$, l'ultimo blocco inizierebbe a $96$ e finirebbe a $128$. 
Il min serve a fermarsi a $100$ per evitare di leggere memoria fuori dai limiti (Segmentation Fault).

- NOTE: for (int k = kk; k < min(kk + B, n); k++)
Questo è il segreto dell'efficienza: a[i][k] e b[k][j] rimangono in cache perché stiamo lavorando su un range di k molto piccolo.

- NOTE: c[i][j] += a[i][k] * b[k][j];
Questa è l'operazione standard. 
La differenza è che, grazie ai cicli sopra, quando il programma chiede al processore il valore di b[k][j], 
è molto probabile che quel valore sia già nella Cache L1 (vicinissima al processore) perché è stato usato un istante prima per il calcolo dell'elemento precedente del tile.


- LOOP TILING (THEORY) 
L'obiettivo è suddividere le matrici in sottomatrici (tile) di dimensione $B \times B$ che possano risiedere interamente nella cache.
Perché questa struttura è più efficiente? 
Nel tuo codice originale (l'ordine $i, k, j$ è già buono perché favorisce il riuso di a[i][k]), il problema sorge con la matrice b[k][j]. 
Quando $n$ è molto grande, ogni volta che il ciclo $k$ avanza, la riga di b caricata precedentemente potrebbe essere già stata espulsa dalla cache.

Con il tiling, ecco cosa cambia:
    - Località dei dati: Invece di scorrere tutta la riga $j$ (lunga $n$), ci fermiamo dopo $B$ elementi. 
                            Il blocco di dati della matrice b rimane "caldo" nella cache mentre il ciclo $k$ itera.

    - Scomposizione del problema: I tre cicli esterni (ii, kk, jj) servono a navigare tra i blocchi, mentre i tre cicli interni (i, k, j) eseguono la moltiplicazione standard 
                                    ma solo su una piccola porzione di dati che "fitta" nella cache L1 o L2.

    - Gestione dei bordi: L'uso della funzione min(ii + B, n) è fondamentale per gestire i casi in cui la dimensione della matrice $n$ non sia un multiplo esatto della dimensione del tile $B$.


- Come scegliere la dimensione del Tile ($B$)?
Idealmente, vogliamo che un tile di ogni matrice sia presente in cache contemporaneamente. 
Se hai 3 matrici (A, B, C), la condizione approssimativa è:
            $$3 \times B^2 \times \text{dimensione\_dato} < \text{Dimensione Cache}$$

Ad esempio, se ogni elemento è un double (8 byte) e hai una cache L1 di 32KB, un valore di $B$ intorno a 32 è spesso un buon punto di partenza ($3 \times 32^2 \times 8 \approx 24KB$).


Il numero di operazioni matematiche non cambia: Per calcolare una matrice $C$ di dimensione $n \times n$ moltiplicando $A$ e $B$, devi calcolare $n^2$ elementi finali. 
Ogni elemento richiede $n$ moltiplicazioni e addizioni. 
Che tu faccia questi calcoli scorrendo la matrice riga per riga, o raggruppandoli a "blocchi" (tiles) di dimensione $B \times B$, il totale delle moltiplicazioni da eseguire alla fine è sempre $n^3$. 
Tu stai solo cambiando l'ordine in cui le esegui. Cosa cambia allora? 
La costante nascosta. In notazione Big-O, il tempo di esecuzione reale si può approssimare come $T = c \cdot n^3$, dove $c$ è una costante che rappresenta il costo (in tempo) di ogni singola operazione. 

- Nel caso della Baseline, $c$ è enorme perché la CPU passa la maggior parte del tempo ad aspettare i dati dalla RAM. 
- Il Loop Tiling abbassa drasticamente il valore della costante $c$ (perché i dati sono già pronti nella velocissima cache L1), permettendoti di andare molto più veloce nel mondo reale. 
Tuttavia, la curva di crescita al variare della dimensione $n$ rimarrà sempre cubica.

La "chicca" da spendere all'esame: Se un professore ti chiede: "E se volessi abbattere quel limite di $O(n^3)$?" La risposta corretta è che non puoi farlo ottimizzando l'hardware. 
Devi cambiare l'algoritmo matematico alla base, usando ad esempio l'Algoritmo di Strassen, che sfrutta proprietà algebriche per ridurre le moltiplicazioni e abbassare la complessità a $O(n^{2.807})$.
Tuttavia, puoi aggiungere che in HPC (e in librerie standard come OpenBLAS o Intel MKL) si preferisce quasi sempre usare l'algoritmo standard $O(n^3)$ iper-ottimizzato con Tiling e vettorializzazione, 
perché Strassen introduce un overhead altissimo, è più difficile da parallelizzare e perde precisione numerica con i numeri in virgola mobile (double).

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

VARIABLE TEMP - REGISTER PROMOTION

Codice lento: c[i][j] += a[i][k] * b[k][j];
Qui il programma deve accedere alla memoria per leggere a[i][k] ogni singola volta che il ciclo j avanza. 
Anche se a[i][k] non cambia mentre j scorre, il compilatore potrebbe non essere sicuro al 100% che sia sicuro tenerlo in un registro (per colpa del "pointer aliasing").

Codice veloce: double temp_a = a[i][k];
Forzi il processore a caricare il valore di A in un registro della CPU (la memoria più veloce in assoluto). 
Nel ciclo interno di j, il computer ora fa solo: RegistroA * ValoreB + ValoreC. Non deve più andare a cercare a[i][k] in memoria.

Questa è un'ottima implementazione di Loop Tiling + Loop Unrolling Manuale (grazie a temp_a).


La Register Promotion è un'importante tecnica di ottimizzazione utilizzata dai compilatori per rendere l'esecuzione di un programma molto più veloce.
In sintesi, consiste nel prendere una variabile o un valore che normalmente verrebbe salvato e letto continuamente dalla memoria centrale (o dalla cache) e "promuoverlo", ovvero spostarlo temporaneamente all'interno di un registro della CPU.
Se il tuo programma ha un'istruzione che aggiorna continuamente lo stesso valore (ad esempio, un contatore dentro un ciclo infinito o un accumulatore), leggere e scrivere quel valore in memoria a ogni singolo passaggio rallenta enormemente l'esecuzione.


TEST CONFERMATI
- CASO NULLO: MEGLIO CON TEMP PER TECNICA DI REGISTRI
- CASO -O3: NESSUNA VARIAZIONE IN QUANTO LO FA IN AUTOMATICO
- PER ICX NON CAMBIA NULLA PERCHE LUI PARTE OTTIMIZZATO CON DI DEFAULT IDEALMENTE

Caso 1: Compilazione SENZA ottimizzazioni (flag -O0)
Se compili il codice omettendo i flag di ottimizzazione (che di default su GCC corrisponde a -O0), noterai una differenza di prestazioni gigantesca a favore del secondo snippet.
- Nel primo snippet: Ad ogni singolo giro del ciclo j, il programma ordina alla CPU di ricalcolare l'indirizzo di memoria della matrice a, di andare a cercare la riga i, la colonna k e di estrarre il valore. 
    Questo genera milioni di letture in memoria (o in cache) totalmente inutili e ridondanti.
- Nel secondo snippet (con temp_a): Leggi il valore dalla memoria una volta sola prima che inizi il ciclo j. 
    Lo salvi in temp_a (che la CPU terrà a portata di mano, probabilmente nello stack primario o in un registro). Il ciclo interno dovrà solo macinare la moltiplicazione, correndo molto più veloce.

Caso 2: Compilazione CON ottimizzazioni (flag -O2 o -O3)
Se compili usando -O3 (come abbiamo fatto nei nostri test precedenti), noterai esattamente zero differenze sul cronometro. I due codici impiegheranno lo stesso identico tempo.
Il motivo è che il compilatore, leggendo il primo snippet, esegue un'analisi del codice:
- Vede a[i][k] dentro il ciclo j.
- Si accorge che i e k non variano rispetto a j.
- Applica automaticamente la Loop-Invariant Code Motion, estraendo lui stesso la lettura di a[i][k] fuori dal ciclo j e mettendola in un registro della CPU.
Di fatto, a livello di linguaggio macchina (Assembly), GCC e ICX trasformano il tuo primo snippet in modo che assomigli esattamente al secondo snippet prima ancora di eseguirlo.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

- REORDERING

Nella moltiplicazione di matrici $C = A \times B$, l'ordine dei tre cicli più interni è il fattore che determina il successo o il fallimento della cache. 
Esistono 6 combinazioni possibili ($ijk, ikj, jik, jki, kij, kji$).
Per l'architettura dei processori moderni (che leggono la memoria in modo sequenziale per righe), la versione più efficiente è quella che hai scritto tu: l'ordine IKJ.

Ecco perché: 
- Matrice A (a[i][k]): Nel ciclo più interno ($j$), l'indice $i$ e $k$ sono fissi. a[i][k] viene caricato una sola volta in un registro (il tuo temp_a). 
- Matrice C (c[i][j]): Nel ciclo più interno, l'indice $j$ scorre linearmente ($j, j+1, j+2 \dots$). Questo è l'ideale per la cache perché sfrutta la località spaziale.
- Matrice B (b[k][j]): Anche qui, $j$ scorre linearmente. Il processore può pre-caricare i dati (Prefetching) perché legge la riga di $B$ in ordine.

La regola d'oro: Il ciclo più interno deve sempre scorrere sull'indice che rappresenta le colonne della matrice (l'indice più a destra, in questo caso $j$), in modo da leggere la memoria in modo contiguo.

Se il professore ti chiede "Perché non $i-j-k$?", la tua risposta, basata su questo testo, sarà devastante:
"Perché in $i-j-k$ il ciclo interno varia la $k$. Quindi b[k][j] salta di riga in riga. 
In C, saltare di riga in riga significa saltare di $N$ posizioni in memoria (stride-$N$). 
Questo carica una cache line da 64 byte per usare un solo double, sprecando gli altri 7 e saturando la banda di memoria inutilmente. Con $i-k-j$ li consumo tutti e 8."

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

- OpenMP / MPI
Nota importante da fare sull'architettura: Note the hybrid core architecture, which presents specific load-balancing challenges for shared-memory parallelization.
